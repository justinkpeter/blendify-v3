export const Stats = ({children}) => {
    return (
        <>
            <div className={' w-full flex h-screen relative content'}>
                {children}
            </div>
        </>
    );
};